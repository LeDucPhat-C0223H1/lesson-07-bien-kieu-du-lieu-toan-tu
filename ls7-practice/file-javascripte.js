var inputWidth;
var inputHeight;

inputWidth = prompt ("Enter the width");
inputHeight = prompt ("Enter the height");

var inputWidth = parseInt(inputWidth);
var inputHeight = parseInt(inputHeight);
var area = inputWidth * inputHeight ;
document.write ("Area =" + area);